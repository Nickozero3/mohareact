import SubirDatos from "./Subirdatos"

const adminPanel = () => {
    return (
        <SubirDatos/>
    );
    }

export default adminPanel;
// This code defines a simple admin panel page using React. It includes a title and a description, styled with Tailwind CSS classes for layout and typography. The page is centered both vertically and horizontally within the viewport, providing a clean and modern look.